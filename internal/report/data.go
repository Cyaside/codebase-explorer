package report

func DataREADME() string {
	return "# Data\n\nThis folder contains machine-readable outputs for the analysis bundle, including `contract.json` for schema tracking and `ai-context.json` for the condensed provider payload.\n"
}
