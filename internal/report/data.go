package report

func DataREADME() string {
	return "# Data\n\nThis folder contains machine-readable outputs for the analysis bundle, including `contract.json` for schema tracking, `ai-context.json` for the condensed provider payload, and `ai-result.json` for structured AI synthesis status or output.\n"
}
