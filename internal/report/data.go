package report

func DataREADME() string {
	return "# Data\n\nThis folder contains machine-readable outputs for the analysis bundle, including `contract.json` for schema tracking, `ai-context.json` for the condensed provider payload, `ai-result.json` for structured AI synthesis status or output, `../changes/issue-correlation.json` for supporting-file correlation, and the viewer payload embedded in `../ui/viewer-data.js`.\n"
}
